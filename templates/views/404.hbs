<!DOCTYPE html>

<html>
    <head>
        <title>Page not found</title>
        <link rel="icon" href="/img/temperature.png">
    </head>

    <body>
        <div class="main-content">
            {{>header}}
            <p>{{errorMessage}}</p>
        </div>
        {{>footer}}
    </body>
</html>